export type Category = {
  id: number;
  name: string;
  description: string;
};

export type Supplier = {
  id: number;
  name: string;
  contactPerson: string;
  phoneNumber: string;
  email: string;
  address: string;
};
